package giftgame;public class GiftGame {	public static void main(String args[]) {				// Make a few gifts and link them together				Gift auntMary = new Gift("Chocolates", null);		Gift uncleBob = new Gift("Lottery ticket", auntMary);				// Remember the head of the chain, then let the relatives		// leave their gifts unattended. The gifts are still accessible		// by following the chain.				Gift headOfChain = uncleBob;		uncleBob = null;		auntMary = null;				System.out.println("The first gift on the chain is");		System.out.println(headOfChain);				// Add another gift to the chain				Gift mom = new Gift("Socks", headOfChain);		headOfChain = mom;		mom = null;				// Now print them all out				System.out.println();		System.out.println("Here are all the gifts");				Gift thisGift = headOfChain;		while (thisGift != null) {			System.out.println(thisGift);			thisGift = thisGift.getNextGift();		}	}}